<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.hunliji.hljlivelibrary"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".activities.LiveMediaPagerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".activities.LiveChannelActivity"
            android:label="@string/title_activity_live_channel"
            android:theme="@style/CustomTitleTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />
        <activity
            android:name=".activities.LiveMessageChatActivity"
            android:theme="@style/DialogActivityTheme"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".activities.LiveWsChatActivity"
            android:theme="@style/DialogActivityTheme"
            android:windowSoftInputMode="stateVisible|adjustResize" />
    </application>

</manifest>